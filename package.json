{
	"name": "pygmysizes",
	"version": "0.0.1",
	"description": "lightweight lazyloading option",
	"source": "src/index.js",
	"main": "dist/index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "microbundle watch -f iife --compress=false",
		"build": "microbundle -f iife --terser"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"microbundle": "^0.11.0"
	}
}
