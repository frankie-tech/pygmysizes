{
	"name": "pygmysizes",
	"version": "0.0.5",
	"description": "lightweight image lazyloading",
	"main": "./dist/pygmysizes.js",
	"exports": "./dist/pygmysizes.modern.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"clean": "rm -rf dist/",
		"watch": "microbundle watch src/index.js -f=modern,umd --name=pygmysizes --no-source-map",
		"build:min": "microbundle src/index.js -f=modern,umd --name=pygmysizes"
	},
	"author": "frankie-tech",
	"license": "MIT",
	"devDependencies": {
		"microbundle": "^0.13.0",
		"npm-run-all": "^4.1.5"
	}
}
