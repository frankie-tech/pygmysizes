{
	"name": "pygmysizes",
	"version": "0.0.1",
	"description": "lightweight lazyloading",
	"main": "./dist/index.js",
	"exports": "./dist/index.modern.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"clean": "rm -rf dist/",
		"build:min": "microbundle src/index.js -f=modern,umd --name=pygmysizes",
		"build:main": "microbundle src/index.js -f=modern,umd --name=pygmysizes --no-compress"
	},
	"author": "frankie-tech",
	"license": "MIT",
	"devDependencies": {
		"husky": "^4.3.7",
		"microbundle": "^0.13.0",
		"npm-run-all": "^4.1.5"
	},
	"husky": {
		"hooks": {
			"pre-commit": "run-s clean build:*"
		}
	}
}
